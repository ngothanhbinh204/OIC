section.section-toggle-change-image(class="section-py")
	.container 
		.wrapper(class="grid xl:grid-cols-2 grid-cols-1 gap-base")
			.col-left 
				.wrap-item-toggle(class='flex flex-col gap-3')
					- for(var i = 0; i < 4; i++)
						.item-toggle(class="group transition-300" data-image-index=i)
							.title(class="flex items-center justify-between cursor-pointer transition-300")
								.toggle-wrapper(class="flex items-center gap-5")
									.number(class="body-1 font-bold text-Primary-1")= (i + 1)
									span(class="font-bold") Phòng học Edufin
								i.fa-light.fa-chevron-down.text-xl.ml-auto(class="transition-300 text-Neutral-White")
							.content(style="display: none;")
								.desc(class="font-normal mt-5 lg:px-16 px-5")
									p Không gian học tập được thiết kế khoa học, bao gồm nhiều phòng học chức năng như học nhóm, hội trường hay phòng học nghe, đặc biệt là Rạp chiếu phim EduFin cho phép học tiếng anh qua điện ảnh.
			.col-right(class="flex flex-col gap-base")
				.img.toggle-image(class="img-ratio ratio:pt-[670_680] rounded-5" data-image-index="0")
					+PugImg()
				.img.toggle-image(class="img-ratio ratio:pt-[670_680] rounded-5" data-image-index="1")
					+PugImg("./img/logoc")
				.img.toggle-image(class="img-ratio ratio:pt-[670_680] rounded-5" data-image-index="2")
					+PugImg("./img/logo.png")
				.img.toggle-image(class="img-ratio ratio:pt-[670_680] rounded-5" data-image-index="3")
					+PugImg("./img/logo.png")